<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Before rotating, immediately set the alpha to 0. -->
    <objectAnimator
        android:valueFrom="@string/valueTo2"
        android:valueTo="@string/valueFrom2"
        android:propertyName="@string/alpha"
        android:duration="0" />

    <!-- Rotate. -->
    <objectAnimator
        android:valueFrom="@string/ValueFrom3"
        android:valueTo="@string/valueTo3"
        android:propertyName="@string/rotationY"
        android:interpolator="@android:interpolator/accelerate_decelerate"
        android:duration="@integer/card_flip_time_full" />

    <!-- Half-way through the rotation (see startOffset), set the alpha to 1. -->
    <objectAnimator
        android:valueFrom="@string/valueFrom2"
        android:valueTo="@string/valueTo2"
        android:propertyName="@string/alpha"
        android:startOffset="@integer/card_flip_time_half"
        android:duration="1" />
</set>

